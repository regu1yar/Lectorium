{
  "name": "lectorium-root",
  "version": "0.1.0",
  "private": true,
  "cacheDirectories": [
    "frontend/node_modules"  
  ],
  "scripts": {
    "postinstall": "cd frontend && npm install && echo 'install is also done by maven'",
    "build": "cd frontend && npm run build && echo 'build is also done by maven'"
  }
}
